/* RuleFilter objects have a single method, filter(), which takes a
   URL Object and an array of Rules as arguments and returns those
   Rules that match the URL */
var RuleFilter = (function () {

})();
