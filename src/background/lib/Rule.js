/* A Rule object consists of its type (domain, path, prefix, regex),
   its state and an array of search & replace pairs. It has methods to
   disable the rule, match it to a string or stringify it.*/
var Rule = (function () {

})();
